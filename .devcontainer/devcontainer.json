{
	"name": "akdc-kind",
	"remoteUser": "vscode",
	"image": "ghcr.io/retaildevcrews/kind:beta",
	"runArgs": [
		"--init",
		"--privileged"
	],
	"overrideCommand": false,
	"settings": { },
	"forwardPorts": [
		30000,
		30080,
		30088,
		32000
	],
	// Install extensions
	"extensions": [
		"davidanson.vscode-markdownlint",
		"gruntfuggly.todo-tree",
		"ms-azuretools.vscode-docker",
		"mutantdino.resourcemonitor",
		"humao.rest-client"
	],
	"remoteEnv":
	{
		"BR_FLUX_FORWARD_NAMESPACE": "flux-cd",
		"BR_GO111MODULE": "on",
		"BR_ISTIO_VERSION": "1.10.2",
		"BR_ISTIO_HOME": "/usr/local/istio",
		"BR_PATH": "${containerEnv:PATH}:$ISTIO_HOME/bin:$HOME/.dotnet/tools"
	},

	"postStartCommand": "alias k='kubectl' && alias kga='kubectl get all' && alias kgaa='kubectl get all --all-namespaces' && alias kaf='kubectl apply -f' && alias kdelf='kubectl delete -f' && alias kl='kubectl logs' && alias kccc='kubectl config current-context' && alias kcgc='kubectl config get-contexts' && alias kj='kubectl exec -it jumpbox -- bash -l' && alias kje='kubectl exec -it jumpbox -- ' && alias ipconfig='ip -4 a show eth0 | grep inet | sed \"s/inet//g\" | sed \"s/ //g\" | cut -d / -f 1' && export PIP=$(ipconfig | tail -n 1) && complete -F __start_kubectl k && echo 'post start' > ~/status",

	// Run Bash script in .devcontainer directory
	"postCreateCommand": "/bin/bash ./.devcontainer/post-install.sh"
}
